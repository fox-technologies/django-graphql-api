### GraphQL with Python via Graphene

## Install

Install Python (any version will do). 

It is recommended you [download the latest version from the Python website](https://www.python.org/downloads/).

Then:

`pip install graphene`

Run:

`python schema.py`
